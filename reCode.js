   // ! to filter and sort by state 
   // const filterBooksHandler = (genre) => {
   //    console.log(
   //       books.filter(book => book.genre === genre)
   //    );
   // }

   // function compare( a, b ) {
   //    if ( a.name < b.name ){
   //      return -1;
   //    }
   //    if ( a.name > b.name ){
   //      return 1;
   //    }
   //    return 0;
   // }

   // const sortBooksHandler = (sortType) => {
   //    if(sortType === 'alphabet'){
   //       setBooks(books.sort(compare))
   //       console.log(books);
   //    }else if(sortType === 'price'){
   //       setBooks(books.sort((a, b) => parseFloat(a.price) - parseFloat(b.price)))
   //       console.log(books);
   //    }else if(sortType === 'popular'){
   //       setBooks(books.sort((a, b) => parseFloat(a.rating) + parseFloat(b.rating)))
   //       console.log(books);
   //    }
   // }